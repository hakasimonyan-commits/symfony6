<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">

        <a class="navbar-brand" href="{{ path('app_home') }}">MyApp</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">

            {# ՁԱԽ ԿՈՂՄ #}
            <ul class="navbar-nav me-auto">
                {% if app.user %}
                {# Catégories — միայն ADMIN / EMPLOYEE #}
                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_EMPLOYEE') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_category') }}">Catégories</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_product') }}">Produits</a>
                </li>

                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_EMPLOYEE') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_employee') }}">Employee</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_employee_users') }}">Liste</a>
                </li>
                {% endif %}
                {% endif %}


            </ul>



            {# ԱՋ ԿՈՂՄ #}
            <ul class="navbar-nav ms-auto">
                {% if app.user %}

                <li class="nav-item">
                    <span class="nav-link fw-bold">
                        {{ app.user.prenom }}
                    </span>
                </li>

                <li class="nav-item">
                    {% if is_granted('ROLE_ADMIN') %}
                    <span class="nav-link text-danger">ADMIN</span>
                    {% elseif is_granted('ROLE_EMPLOYEE') %}
                    <span class="nav-link text-primary">EMPLOYEE</span>
                    {% else %}
                    <span class="nav-link text-secondary">USER</span>
                    {% endif %}
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>

                {% else %}

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                </li>

                {% endif %}
            </ul>

        </div>
    </div>
</nav>