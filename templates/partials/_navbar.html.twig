<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">

        <a class="navbar-brand" href="/">MyApp</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">

            <!-- ձախ կողմ -->

            <ul class="navbar-nav me-auto">
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_category') }}">Catégories</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_product') }}">Produits</a>
                </li>
                {% endif %}
            </ul>
            {% if app.user %}
            {% if is_granted('ROLE_ADMIN') %}
            <span class="navbar-text ms-2">ADMIN</span>
            {% elseif is_granted('ROLE_EMPLOYEE') %}
            <span class="navbar-text ms-2">EMPLOYEE</span>
            {% else %}
            <span class="navbar-text ms-2">USER</span>
            {% endif %}
            {% endif %}

            <!-- աջ կողմ -->
            <ul class="navbar-nav ms-auto">
                {% if app.user %}
                <li class="nav-item">
                    <span class="nav-link">
                        {{ app.user.prenom }}
                    </span>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Se Déconecter</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                </li>
                {% endif %}
            </ul>

        </div>
    </div>
</nav>